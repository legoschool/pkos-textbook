# 자료 변환기 — 필요한 것들
#   python 변환.py --점검   으로 무엇이 빠졌는지 확인할 수 있습니다.

pymupdf>=1.24        # PDF 읽기 + PDF 속 그림 꺼내기
python-docx>=1.1     # 워드 .docx
python-pptx>=0.6     # 파워포인트 .pptx
openpyxl>=3.1        # 엑셀 .xlsx
olefile>=0.46        # 한글 .hwp, 옛 .doc/.ppt/.xls 껍데기 열기
xlrd>=2.0            # 옛 엑셀 .xls  (xlrd 2.x 는 .xls 전용이라 딱 맞음)
beautifulsoup4>=4.12 # 웹문서 .html
pillow>=10.0         # 그림 형식 바꾸기(BMP·TIFF → PNG) · 자가진단의 시험 그림 · 글자 인식

# 없어도 되는 것 ─────────────────────────────────────────
# pytesseract           사진 글자 인식(OCR) 을 tesseract 로 켤 때만
# anthropic             Claude 비전 OCR 을 켤 때만

# 라이브러리 없이 읽는 것 ────────────────────────────────
#   한글 .hwpx (ZIP+XML), 텍스트, 마크다운, CSV/TSV,
#   JSON, 주피터 노트북, 자막, 소스코드, 사진 EXIF,
#   ZIP 압축 풀기 — 전부 파이썬 표준 기능만 씁니다.
